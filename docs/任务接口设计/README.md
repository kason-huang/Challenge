# 任务接口设计文档目录

## 概述

本目录包含任务接口设计的完整分析文档，从接口设计的角度系统分析了任务对象应该包含的核心概念。

## 文档列表

### [任务接口设计分析.md](./任务接口设计分析.md)

完整的任务接口设计分析文档，包含以下内容：

#### 主要章节

1. **任务接口的概念层次**
   - 第1层：核心概念 (BaseTask)
   - 第2层：任务类型抽象
   - 第3层：扩展概念

2. **核心概念定义**
   - Episode 规范
   - BaseTask 核心接口
   - 场景配置、任务规范、交互接口、空间定义、评价标准

3. **任务类型抽象**
   - ManipulationTask（操作任务）：低层关节控制
   - NavigationTask（导航任务）：高层运动原语

4. **操作任务 vs 导航任务对比**
   - 动作空间、观测空间、奖励函数对比
   - 接口概念对比表

5. **统一接口设计**
   - ActionProcessor：动作处理器抽象
   - StateTracker：状态跟踪器
   - RewardShaper：奖励塑造器
   - TrajectoryGenerator：轨迹生成器

6. **实现示例**
   - 简单抓取任务
   - 点导航任务
   - 混合任务（抓取+导航）

## 核心概念对比

| 概念类别 | 核心概念 | 操作任务 | 导航任务 |
|---------|---------|---------|---------|
| **环境** | Scenario | 机器人+物体+场景 | 机器人+场景+地图 |
| **目标** | Goal Spec | 物体目标位姿 | 目标位置/区域 |
| **动作** | Action Space | 关节位置/速度 | 运动原语/速度 |
| **观测** | Observation | 关节+物体+视觉 | 位姿+地图+视觉 |
| **评价** | Reward/Termination | 稠密/多阶段 | 稠密或稀疏 |
| **处理** | ActionProcessor | 关节控制映射 | 运动命令映射 |
| **状态** | StateTracker | 抓取/接触状态 | 位姿/目标距离 |
| **奖励** | RewardShaper | 距离/接触/成功 | 距离/时间/成功 |

## 设计原则

1. **分层抽象**：通过 ActionProcessor 解耦动作表示
2. **统一状态**：通过 TensorState 统一状态表示
3. **模块化**：通过组合模式支持复杂任务
4. **扩展性**：支持自定义任务类型和处理器

## 核心思想

通过**分层抽象**和**组合模式**，让不同任务类型共享核心接口，同时在各自的专业领域保持灵活性。
